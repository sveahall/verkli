// Stub for the "server-only" package used by Next.js.
// In the real app, importing "server-only" from a client component causes a
// build error. In Vitest we just need a no-op module so imports resolve.
export {};
